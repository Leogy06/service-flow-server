services:
  # redis is already running on the host (container: redis, port 6379) — reusing it
  # See docker ps — no need to start our own

  redis:
    image: redis:latest
    container_name: redis-main
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
